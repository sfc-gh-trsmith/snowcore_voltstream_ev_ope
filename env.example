# =============================================================================
# VoltStream EV OPE - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# The .env file is gitignored and will not be committed.
#
# Usage:
#   cp env.example .env
#   # Edit .env with your actual values
#   source .env  # Or let deploy.sh read it automatically

# =============================================================================
# Snowflake Connection (for sf_cortex_agent_ops.py)
# =============================================================================
# These are optional if using Snow CLI connection profiles.
# Required for direct script usage without Snow CLI.

# SNOWFLAKE_ACCOUNT=your-account-identifier
# SNOWFLAKE_USER=your-username
# SNOWFLAKE_WAREHOUSE=VOLTSTREAM_EV_OPE_WH
# SNOWFLAKE_ROLE=VOLTSTREAM_EV_OPE_ROLE

# =============================================================================
# Authentication
# =============================================================================
# Option 1: Private Key (recommended for SQL operations)
# SNOWFLAKE_PRIVATE_KEY_PATH=/path/to/your/private_key.p8
# SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=optional-passphrase

# Option 2: PAT Token (required for Cortex Agent REST API operations)
# Generate in Snowsight: User Menu > Security > Programmatic Access Tokens
SNOWFLAKE_PAT_TOKEN=your-programmatic-access-token-here

